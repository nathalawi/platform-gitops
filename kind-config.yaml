# kind create cluster --name platform-gitops --config kind-config.yaml
# Create network first
# docker network create \
#   --driver bridge \
#   --subnet 172.21.0.0/16 \
#   kind
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
- role: control-plane
  extraPortMappings:
  - containerPort: 80
    hostPort: 8080
  - containerPort: 443
    hostPort: 8443
- role: worker
- role: worker